# =============================================================================
# Geopard Server Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your Azure credentials
# Command: cp .env.example .env

# Azure OpenAI Configuration
# Get these from: https://portal.azure.com → Your OpenAI Resource
AZURE_OPENAI_API_KEY=your_openai_api_key_here
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/
AZURE_OPENAI_API_VERSION=2024-12-01-preview

# OpenAI Models
# Embedding model for RAG vector search
EMBEDDING_MODEL=text-embedding-3-large

# Chat model for responses (function calling support required)
CHAT_MODEL=gpt-4o

# Azure AI Search Configuration
# Get these from: https://portal.azure.com → Your Search Service
AZURE_SEARCH_ENDPOINT=https://your-search-service.search.windows.net
AZURE_SEARCH_KEY=your_search_admin_key_here
AZURE_SEARCH_INDEX_NAME=geopard-rag-v2

# Optional: Server Configuration
# SERVER_PORT=8000
# SERVER_HOST=0.0.0.0
# LOG_LEVEL=info

# Optional: MCP Configuration
# MCP_SERVER_ENABLED=true

# =============================================================================
# IMPORTANT NOTES:
# =============================================================================
# 1. Never commit the .env file with real credentials to version control
# 2. The .env file should be listed in .gitignore
# 3. Make sure your Azure OpenAI deployment supports function calling
# 4. Verify your Search index name matches the one created during setup
# 5. Keep your API keys secure and rotate them regularly
